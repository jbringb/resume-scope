# Copy to .env for docker compose. For local vLLM: SPRING_PROFILES_ACTIVE=local-vllm
OPENAI_API_KEY=change-me
# Host root only (no /v1). Spring AI adds /v1/chat/completions.
# App on host: http://localhost:8000
# App in Docker, vLLM on host: http://host.docker.internal:8000
OPENAI_BASE_URL=http://localhost:8000
OPENAI_MODEL=Qwen/Qwen3-4B-Instruct-2507
